<!DOCTYPE html>
<html>
  <head>
    <title>Hitch</title>
    <link rel='stylesheet' href='/stylesheets/style.css'/>
    <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
    <script src="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>
  </head>
  <body> 
  	<% if (typeof(e) !== 'undefined') { %>
      <div id='error'> <%= e %> </div>
    <% } else if (loggedIn === false) { %>
      <h2>Hitch</h2>
      <a href='/users/login'>Log in</a> | <a href='/users/register'>Register</a>
    <% } else { %> 
    <h2>Welcome, <%= user.kerberos %></h2>

    <div class="container">
      <h2>Open rides</h2>        
      <table class="table">
        <thead>
          <tr>
            <th>Departure time</th>
            <th>Start Location</th>
            <th>Destination</th>
            <th>Seats open</th>
            <th>Transport</th>
          </tr>
        </thead>
        <tbody>
          <% for (var i = 0; i < rides.length, i ++) {%>
            <tr>
              <td>rides[i].departure_time</td>
              <td>rides[i].origin</td>
              <td>rides[i].destination</td>
              <td>rides[i].remaning_capacity</td>
              <td>rides[i].transport</td>  
              <form method="post" action="/rides/participate/:ride">
                <% if (rides[i].riders.indexOf(user._id) < 0) {%>
                  <td> <button type="submit" class="btn btn-default">Join ride</button> </td>
                <%} else { %>
                  <td> <button type="submit" class="btn btn-default">Leave ride</button> </td>
                <% } %>
              </form>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>

  </body>
</html>
